export type CursorMode = 'number' | 'note';

export type Size = {
    width: number,
    height: number,
}

export type Point = {
    x: number,
    y: number,
}

export type GridPosition = {
    row: number,
    col: number
};